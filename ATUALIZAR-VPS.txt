AVADESK — atualizar sozinho (copiar e colar)
============================================

Site no ar:
  https://suporte.avadesk.com.br/login

Cloudflare (ja configurado — NAO apagar, NAO criar chamados.*):
  Hostname: suporte.avadesk.com.br
  Path:     *
  Origin:   http://127.0.0.1:3105

NAO use no navegador: IP da VPS, :3105, chamados.avadesk.com.br, avadesk.com.br
NAO rode:  bash deploy/avadesk-deploy.sh   (reescreve .env e pode apagar WEB_ORIGIN)
NAO recrie o container avadesk-pg. NAO rode pm2 stop all.

------------------------------------------------
1) Neste PC — mandar o codigo para o GitHub
------------------------------------------------
cd d:\avadesk\avadesk
git add -A
git status
git commit -m "atualizacao"
git push origin main

Sem git push, a VPS NAO recebe o que esta no Cursor.

------------------------------------------------
2) SSH na VPS
------------------------------------------------
ssh root@179.199.149.12

------------------------------------------------
3) Cole este bloco inteiro (ja logado como root)
------------------------------------------------

set -euo pipefail
cd /opt/avadesk
test -d .git
test -f .env
test -f package.json
git pull --ff-only
docker start avadesk-pg >/dev/null 2>&1 || true
for _ in $(seq 1 40); do
  if docker exec avadesk-pg pg_isready -U avadesk >/dev/null 2>&1; then
    echo PG_READY
    break
  fi
  sleep 2
done
docker exec avadesk-pg pg_isready -U avadesk
npm ci
npm run build
npm run migrate
npm run seed
pm2 restart avadesk-api avadesk-web --update-env
pm2 save
echo "--- git ---"
git log -1 --oneline
echo "--- pm2 ---"
pm2 ls --no-color
echo "--- health ---"
curl -sS http://127.0.0.1:4105/health || true
echo
curl -sSI http://127.0.0.1:3105/login | head -15 || true
echo "--- docker ---"
docker ps --filter name=avadesk-pg --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
echo DEPLOY_UPDATE_DONE

------------------------------------------------
4) Conferir
------------------------------------------------
Apareceu DEPLOY_UPDATE_DONE.
Abra https://suporte.avadesk.com.br/login  (Ctrl+F5)

Atalho se o script ja estiver na VPS:
  bash /opt/avadesk/deploy/avadesk-update.sh

Copia neste PC:  d:\avadesk\ATUALIZAR-VPS.txt
Copia no repo:   d:\avadesk\avadesk\ATUALIZAR-VPS.txt
Copia na VPS:    /root/ATUALIZAR-AVADESK.txt
